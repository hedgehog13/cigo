{
  "name": "cigo-test",
  "version": "1.0.0",
  "dependencies": {
    
  },
  "scripts": {
    "start": "cd server && npm install && node server.js",
    "heroku-postbuild": "cd client && npm install && ng build --aot --configuration=production"

  },
  "engines": {
    "node": "10.15.3",
    "npm": "6.4.1"
  }
}
